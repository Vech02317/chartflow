# 开发日志

**给未来的自己(或接手的人)看的。** 这不是使用说明 —— 那两份在 `README.md`(给 agent 读,
是程序的一部分)和 `使用说明.md`(给人类用户读)。这份记的是三件另外的事:

1. **为什么长成现在这样** —— 形制背后的判断,不写下来就会有人(包括未来的我)把它"改好"
2. **踩过哪些坑** —— 尤其是那些踩过一次还会再踩的技术坑
3. **哪些事没做完** —— 搁置项,连"为什么搁置"一起记

时间:2026-09。起因:给自己的毕设出图,做着做着变成了通用工具。

---

## 一、这是什么

一条论文产图流水线:交**论文 + 部分项目源码**,产出 **ER 图 / 实体属性图 / 类图 / 流程图**。
任何 agent(codex / Claude Code / 其他)按 `README.md` 操作即可。

**设计支点 —— 三角色分工,越界即错:**

| 角色 | 做什么 | 不做什么 |
|---|---|---|
| Agent(LLM) | 读论文与源码、产出清单与 JSON | 不决定论文该有哪些图;不手写绘图代码 |
| 确定性引擎 | 校验 / 渲染 / 验收 | **不含任何 LLM**;同输入必同输出 |
| 人类 | 交材料、确认清单、审数据、挑毛病 | 不写代码、不装环境 |

整条流水线里 **LLM 只出现在"抽取"这一步**,其余全部可重跑。

**三条铁律**(写在 `README.md` §3,是产品说明书里要写死的):
① 第一产出是《出图清单》`plan.json`,不是图;② 验收 `PASS` ≠ 正确;③ 不编造,拿不准的进「待确认」。

---

## 二、时间线(按 commit)

| commit | 做了什么 |
|---|---|
| `cad179a` | slice1:ER 跑通 —— 契约校验 + graphviz 渲染 + 自动验收 |
| `53d0be5` | 产品化:三图型(ER/类图/流程图)+ 契约/提示词/双文档/环境自检 |
| `ab1bd14` | ER 出详细/紧凑双版;流程图形状对齐 **GB/T 1526**;版面尺寸门禁 |
| `094e046` | 属性图(陈氏记法)成为第 4 图型 |
| `9f25650` | 属性图:不画联系(外键/多对多);修正起始角目标 |
| `a600ddf` | 类图:支持 UML 2.5 可见性记号 `+ - # ~` |
| `e6475b5` | ER 按模块拆图(`modules`/`module` + 引用框 + 分区与关系全覆盖验收) |
| `e7c2e11` | 提示词:补齐 7 处抽取歧义口径(真数据 codex 实测暴露) |
| `4078537` | 「待确认」落盘:agent 写 `figs/<id>.待确认.md`,`build.py` 汇总成 `out/待确认.md` |
| *(本次提交)* | 提示词:补 2 处(中文名 label 的来源 / 论文内部模块口径冲突);**新增本文件**。用 `git log --oneline -- 开发日志.md` 定位 |

规模:主干 Python 约 1200 行,4 份契约 + 5 份提示词 + 3 套样例。

---

## 三、几个想清楚了才敢写的决策

这几条**看着奇怪,但都是有理由的**。要改之前先看理由。

### 1. 第一产出为什么是清单,不是图

全自动无人审 = 图会**整整齐齐地错**。清单是**唯一的人工闸门**:章节 → 图型 → 源材料 → 图题,
人点头之后才逐图执行。跳过这步省下的时间,会在答辩时还回去。

### 2. 为什么不给契约的 `fields` 开"可以没有"的口子

ER 契约要求每个实体必须有 `fields`,且 `minItems: 1`。做模块图时很想给"外部实体"开个口子
(它只画名字不画字段),但**没开** —— 一旦允许,`fields: []` 就成了**所有**实体的合法写法,
这堵墙(全仓最重要的数据完整性约束)会整体塌掉。

解法改成:外部实体**不写进 JSON**,由渲染器**自动**画成虚线引用框。数据里只有真实体,
每个都必须有字段。**约束不用松,需求也满足了。**

### 3. 为什么「待确认」必须落盘成文件

验收脚本只能证明「图与契约一致」,证明不了「契约与事实一致」。那些要人拍板的东西
**才是这个工具唯一的人机接口**。它要是只活在 agent 的对话里,会话一结束(或上下文一压缩)
就没了,"人拍板"这一步就断了。

**汇总刻意交给 `build.py` 而不是 agent** —— 让 agent 读改写一个大文件,正是最容易覆盖丢失的操作。

### 4. 为什么关系归 `to` 端(外键所在端),不是"任一端"

模块拆图时,第一版按 `from in internal or to in internal` 过滤,**accounts 模块被撑到 983pt** ——
因为 `User` 既属于 accounts 又是全图枢纽,每一条 User 发出的关系都被判成"属于 accounts",
和拆图的目的正好相反。改成 **`to` 端所属模块**后:实测 21 条关系恰好各归一个模块,无重复无遗漏。

> **这条是量出来的,不是想出来的。** 只有跑真代码才会发现"任一端"是错的。

### 5. 为什么尺寸超限只 WARN 不 FAIL

插入 A4 是**美学问题**,数据完整才是**正确性问题**。两者混在一起会让 FAIL 失去意义。
所以:尺寸只警告(并给出可执行的下一步),分区完整性/关系全覆盖才 FAIL。

---

## 四、踩过的坑(技术,别再踩)

| 坑 | 真相 |
|---|---|
| Graphviz `plaintext` 形状下 `style="dashed"` 不生效 | 引用框必须换 `shape="box"`,不能只加 style |
| `<br/>` 拼多行标签,Graphviz 会**居中**,`align="left"` 压不住 | 必须包一层**单列嵌套表**才左对齐 |
| 类图给可见性记号**单开一列** | HTML 表格会把外层富余宽度分给窄列,**同一张图内**属性表和方法表被撑开的程度都不同(实测 14pt vs 55pt),`fixedsize` 压不住 → 记号**拼进成员串**,整行一个文本对象 |
| neato 在 `-n` 下若设 `overlap=false` | 会**缩放整个布局**,算好的坐标全乱。自带布局的图型(attr)绝不能设 overlap/splines/rankdir |
| 属性图挑起始角只压宽度 | 会选中一根 60×336pt 的竖条。目标必须是「**先压最长边、再压宽度**」 |
| 用 dot 的 `concentrate` 合并边 | 真实数据里有语义不同的**平行边**(用户→好友:发起 / 被加),合并就是**静默丢数据** |
| 模块图的 `relation_ids` 用 `(from,to)` 去重 | 同上,平行边会被丢掉 → **必须用原始数组下标** |
| `engine/check_figs.py` 单独跑报 ImportError | README §7 承诺它能单独跑,得自己把仓库根加进 `sys.path` |
| `build.py` 遇到契约不符吐 Python traceback | 包一层 try/except,改成 `[figN] FAIL —— 不符契约 …`(仓库风格是"失败给人话") |

---

## 五、验证做了什么

**方法:自包含测试台。** 只放 `README + contract + prompts + engine + samples + 源码`,
**绝不放该项目自己的 `figs/`**(否则 agent 抄答案)。让它写到隔离目录,
再用 `build.py <隔离目录>/plan.json` 验收(`plan.json` 可放任意目录,契约与引擎从仓库根读)。

**验证过的三轮:**

| 轮次 | 材料 | 结果 |
|---|---|---|
| 1 | 真毕设(Django,booknotes) | 暴露 **7 处提示词歧义**。最要命的一条:N:M 的 `from`/`to` 无定义 → **3 条 N:M 全反了** |
| 2 | 同上,补完提示词后 | **9 项检查全部落地**:N:M 3/3 一致、关系结构 21/21 零差异 |
| 3 | **陌生项目**(SpringBoot + MyBatis + Vue,教务系统,无建表 SQL) | ER **零误差**:16/16 实体字段逐条 == mapper 的 `Base_Column_List`、20/20 关系方向与 `fk_field` 全对、JOIN 别名字段一个没混入、无表的类(基类/视图)没进图。暴露 **2 处新缺口**(已修,见 §六) |

**第 3 轮的诚实附注**:那轮比第 1 轮**简单** —— MyBatis 的外键**明文写在 SQL 的 `join` 里**,
不用像 Django 那样推。那份源码"无文档但**自我描述**"。所以零误差不能全记在提示词头上。

**结论**:提示词做到了「**规则管得到的地方零分歧,管不到的地方 agent 主动列进"待确认"**」。
第 3 轮唯一没报出来的是**字段中文标签的来源**(见 §六),而它的编造面不小
(16 实体 × 平均 8 字段)—— 那是三轮里唯一"做了但没说"的一类。

---

## 六、已知问题 / 搁置项

**搁置不是忘了,是权衡过。** 每条都记了"为什么搁置"和"要做的话怎么做"。

### 提示词(第 3 轮暴露,`22d4c0b` 已修)

- ✅ **字段中文标签的来源没有规则** —— 论文列名与源码列名常常不同(实测:`xuesheng_username`
  vs `username`,连列数都不一样),agent 会**跨两套列名人工对齐**、论文没有的列**按英文意译**,
  两件事原本都不要求上报。已新增三档规则(源码中文 / 论文中文 / 意译),后两档必须报,
  判据是「**人能拿它对着源码逐字核吗**」。
- ✅ **论文内部有两套模块口径** —— 同一篇论文第4章写"三大模块"、第5章是五个功能模块,
  原规则只覆盖"源码 vs 论文"。已补:选定哪套 + 为什么 + **没选的那套也要写出来**。

### 代码(未做)

- **`engine/render_er.py`:模块图里没有引用框时,图例照印。** 症状:某个模块的实体之间没有外键
  关系时,图上三个孤立盒子配一行「虚线框 = 其他模块的实体」的假图例。**两行代码,按需印即可。**
- **类图 / 流程图没有拆图机制。** ER 超 A4 有 `_mod_*` 兜底,class/flow 只能 WARN + 让 agent
  求人拍板(第 3 轮实测:类图 1272×1592pt 需缩 37%、流程图 851×1572pt 需缩 45%)。
  要做的话,可仿 ER 的"同一份数据多版式"(`emit_many`),但类图的拆分轴不像 ER 那么自然。

### 查证过但决定不做

- **「ER 图连线不许交叉」—— 这条查过了,没有依据,也不该写死。**
  - 全仓条文里从来没有过这条约束
  - **没有国标**:ER 图无 GB 标准(GB/T 1526 只管流程图)
  - 中文"绘制规范"类文章把交叉列为**优化点**而非规范条款,措辞是"避免连线**过多**交叉"
  - 英文 best practice 说 "avoid crossings **whenever possible**",但明确承认密集图里
    **"some crossings are unavoidable, especially when many entities relate to one or a few
    central entities"** —— 正是本项目的形态(学生 / 教师 / 课程三个枢纽)
  - 且明确说这类取舍**是 heuristic,不是 enforced hard rule**;理论上最小化边交叉是 **NP-Hard**
  - **结论**:写死等于立一条没人能履约的条文(引擎保证不了,agent 没有杠杆)。
    要管的话写成**可测量 WARN**,阈值可借业界的 "two crossings acceptable; six is a tangle"。
  - 业界对密集图的六条对策,本项目做了 3.5 条(拆图 / 属性分离 / 图例 / 枢纽重复表示半做),
    **线跳、颜色编码、正交折线没做**。

- **纯文字输入(只给一段文字,不给源码)出关系图。** 问了、分析了、**没做**。
  - **渲染那一半天然具备** —— 渲染器只吃 JSON,与输入来源无关;紧凑版本来就只画实体名+关系
  - **抽取那一半不具备** —— 四份 `extract_*.md` 的规则全是"读代码"的;且 ER 契约的
    `fields minItems:1` 直接挡住(纯文字抽不出字段清单)
  - **更关键的是性质会变**:现在是「源码 → 图」的**翻译器**(有事实可核,`PASS` 与「待确认」
    才有意义);纯文字就成了「文字 → 图」的**重述器**,没有 ground truth,
    铁律②那套"人对着源码审"的验收机制**直接失效**
  - 真要做:**别改 `er` 契约**(会为了一类新输入塌掉现有约束),按 `README §9` 加一个新图型
    (如 `erd_light`:只画实体+关系、无字段)。**真正的活不在代码,在重写「待确认」的语义** ——
    要从"源码里查得到/查不到"变成"文字里明说的 / 你推断的",这是两种不同的东西
  - **顺带**:`flow` 图型**本来就是从正文抽的**,所以"给一段文字出流程图"今天就是成立的

### 遗留的小不一致

- `prompts/extract_attr.md` 规则 2 写主键 label 取"XX编号",但真实数据**两次都选了"XXID"** ——
  规则与数据不一致(用户决定不改)
- `work/booknotes/figs/` 里的 `admin_panel` 表名是**不可核证的推断**(源码 `Meta` 里只有
  `verbose_name`、没有 `db_table`)

---

## 七、怎么接手

**读的顺序**(别从代码开始读):

1. `使用说明.md` —— 先看这东西**给谁用、怎么用**
2. `README.md` —— 给 agent 的操作手册,也是这个仓库的**规格说明**
3. **本文件** —— 为什么这样、坑在哪、什么还没做
4. 代码:`contract/`(数据标准)→ `prompts/`(agent 的工作说明书)→ `engine/`(渲染与验收)

**动手前先跑回归基线**(README §4 自测块):

```bash
python3 doctor.py
python3 plan.py  samples/library/plan.json
python3 build.py samples/library/plan.json     # 期望 4/4 PASS
python3 build.py samples/springboot/plan.json  # 期望 1/1(Java 可见性记号)
python3 build.py samples/campus/plan.json      # 期望 1/1(ER 模块图)
```

**改提示词之后怎么验**:搭自包含测试台(见 §五),拿一份**你从没碰过**的真项目跑一遍 codex。
在自己熟悉的数据上调提示词是**自欺** —— 你知道答案,会不自觉地补上提示词的漏洞。

**测试台已经不在了** —— 2026-09-13 收尾时连第 3 轮的全部输入产物一起删掉了
(测试文件不留)。要重建,按 §五 的方法来:

1. 复制一份**自包含副本**:`README.md` + `contract/` + `prompts/` + `engine/` +
   `build.py`/`plan.py`/`doctor.py` + `samples/` + `.venv/`,再加你要验的那份源码
2. **绝不放该项目自己的 `figs/`** —— 那是答案
3. 在副本里开一个 `work/` 目录,让 agent 把 `plan.json` / `figs/` 写在那儿
4. `./.venv/bin/python build.py work/plan.json` 验收

第 3 轮那份里**刻意扣掉了原论文那张 ER 图**(图4-5),以防 agent 直接抄 —— 重建时照办。
另外记得在 `~/.codex/config.toml` 给新目录加一条 `[projects."<路径>"] trust_level = "trusted"`,
否则 codex 每次都会问。

**真毕设产物**:`work/booknotes/out/`(gitignored)—— 21 张 PNG,`fig1`~`fig16` +
`fig1_compact` + 4 张 `fig1_mod_*`,即论文要用的图。
