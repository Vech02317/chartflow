# 使用说明(给人看的)

这是一个**论文出图工具**:你把论文和项目源码交给 AI 助手,它帮你画出规范的 ER 图、实体属性图、类图、流程图。

你**不需要**学编程、装环境、写代码。全程你只做三件事:**给材料、点头、挑毛病**。

---

## 一、你需要准备什么

1. **一篇论文**(或至少你要出图的那几章)
2. **部分项目源码**:数据库建表语句 / 模型代码(出 ER 图、实体属性图)、类定义(出类图)、描述业务流程的正文段落(出流程图)
3. **一个能读文件、能执行命令的 AI 助手**(如 Claude Code / Codex)——抽取那一步必须靠它,没有它就出不了图

## 二、三步走

**第 1 步:交材料,让它先给清单**
把论文和源码交给 AI,说:

> "按 README 的流程,先给我出图清单。"

它会先给你一份 **`plan.json`** —— 列出:哪一章该出什么图、依据是哪个文件、图题叫什么。

**第 2 步:看清单,点头或改**
这是**你最重要的一步**。检查:
- 论文需要但漏了的图 → 让它加
- 它列了你没有材料的图 → 让它删或标"跳过"
- 图题不合规范 → 直接说怎么改
- **实体很多的 ER 图** → 看它有没有建议按模块分开出,以及**分得对不对**
  (是按你论文里的功能模块分的,还是按代码目录分的?你论文怎么写,就让它怎么分)

清单对了,再让它继续。

**第 3 步:收图 + 审数据**
它会跑完流程,把图放在 `out/` 文件夹(`.png` 直接能用进 Word,`.svg` 放大不糊)。

**先看 `out/待确认.md`** —— 那是 AI 自己列出来的、"我拿不准、要你拍板"的地方。
一条条看过去,拿主意就行。这份文件每次跑都会重新生成,**不会丢**。

**然后请你务必做一件事:对着你的源码/建表语句,检查图里的字段和关系对不对。**
电脑能保证"图没画错",但保证不了"数据抽得对"——这一步只能人来盯。

> **实体属性图要特别盯一眼**:它一张图只画一个实体,**只列这个实体自己的字段** ——
> 外键(如"所属书")不算,那些归 ER 图管。主键会带下划线、多值属性画成双椭圆,
> 这些是标准画法不用改。你要是发现少了哪个字段、或多了不该有的,直接说。

> **类图也要盯一眼**:类成员前面带 `+`(公开)`-`(私有)`#`(保护)记号才算合规。
> 但**如果你的源码是 Python,图里就不该有这些记号** —— 那是故意的,不是漏了。
> 这两件事都只有你对着源码能判断。

> **ER 图里可能有虚线框**:实体多时,一张图塞不进论文,工具会自动按模块拆成几张。
> 每张里**别的模块的实体画成虚线框**(只写名字、不列字段),是正常的 ——
> 它只是告诉读者"这条关系连到哪个实体",字段在那个实体自己的模块图里。
> 但如果**整体图**(那张包含所有实体的)里出现了虚线框,那就是漏标了,让它改。

## 三、怎么判断"好不好"

| 它说 | 意思 |
|---|---|
| `PASS` | 图和数据的格式对上了。**不等于内容正确** |
| `FAIL` | 某事没对上,它会说哪一条,让它改就行 |
| "待确认" | 它拿不准的地方,**你要看这一条** |

## 四、你不需要做的事

- ❌ 不用装 Python、Graphviz 等环境 —— 让 AI 装(可能要你输一回密码)
- ❌ 不用改 JSON、不用写代码 —— 哪里不对,用大白话告诉 AI
- ❌ 不用手工修图 —— 数据改了重跑一次,所有图自动更新

## 五、什么时候该找人帮忙

- 图**排得很挤、很难看** —— 是布局参数问题,让 AI 调;多次调不好再找人
- 论文要的图**不在支持范围**(界面截图、时序图)—— 工具出不了,得用别的办法
- 你的 AI 助手**跑在不能联网的环境** —— 装环境那步会卡住,需要有人先手动装一次

## 六、常见问题

**问:它会自己读完整篇论文吗?**
答:不该。按图型只喂相关片段更准也更省。让它"先看目录、出清单"即可。

**问:出的图能直接放进论文吗?**
答:可以。图题已写在图上,风格统一;建议用 `.png` 插进 Word。

**问:为什么多出几张 `_mod_` 的 ER 图?**
答:实体太多,一张图缩太小会看不清,所以工具按模块拆成了几张。
**两张都要插**:第4章先放整体图画个总览,后面每个模块各放一张。
带 `_mod` 的那几张才是给你插论文用的 —— 插它们,别插那张挤成一团的。

**问:改了数据库设计,图会自己更新吗?**
答:不会自动,但重跑一次就全更新 —— 这正是它比手画强的地方。
