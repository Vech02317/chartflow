- 关系类型(继承 / 组合 / 聚合 / 关联 / 依赖)是按源码的成员声明推断的。**最容易错的是
  「组合」与「聚合」的界限** —— 本图按"生命周期是否一致"判,请对着源码复核。
- 类数超过 15 个时只画核心类;本图 9 个类是否已经是最该画的那批,请对照 `classes.py` 看。
- getter/setter / `__str__` 这类样板成员按规则省略了;若论文要求列出,告诉我。
