- **"年龄"画成了派生属性(虚线椭圆),但它对应的列是 `birth_date`** —— 实际存储的是
  "出生日期","年龄"是算出来的。现在图里**没有**"出生日期"这个属性。
  若论文要求把两者都列出,请告诉我补上。
- **"联系电话"画成了多值属性(双椭圆)** —— 依据是 `models.py` 里的注释"一个读者可登记多个号码",
  但源码实现是**单个 `CharField` 存逗号分隔串**。这是"概念"而非"实现",需你拍板。
- 主键 `id` 取"读者编号";`card_no` 的 `verbose_name` 是"借书证号",两者不撞车。
- 外键与本实体无关,未画(按规则,联系归 ER 图)。
